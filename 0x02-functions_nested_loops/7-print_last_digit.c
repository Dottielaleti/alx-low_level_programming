#include "main.h"
/**
 * main - function that prints the last digit of a number
 *
 * Return: last digit
 */
int _lastdigit(n)
{
        int lastdigit = -( n % 10);
        if ( n < 0 )
        {
                putchar( lasdigit + '0')
                        return (lastdigit)
        }
}
