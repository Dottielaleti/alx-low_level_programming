#include "main.h"

/**
 * main - function that prints the alphabet, in lowercase
 *
 * Return: Always 0
 */
void print_alphabet(void)
{
	char = 'a'

	 for('a' <= 'z')
	{
		_putchar(a);
		a++;
	}
	_putchar('\n');
	
	return (0);		
}
