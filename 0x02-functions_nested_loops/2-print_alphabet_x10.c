#include "main.h"

/**
 * void print_alphabet_x10 - prints 10 times the alphabet, in lowercase
 *
 * Return: Always 0
 */
void print_alphabet_x10(void)
{
	char ch;
	int t;

	while (t <= 10)
	{
		ch = 'a'
		while (ch <= 'z')
		{
			_putcher (ch);
			ch++;
		}
		_putcher ('\n');

		t++;
		
		return (0);
	}
 }
