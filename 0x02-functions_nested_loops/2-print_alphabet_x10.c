#include "main.h"

/**
 * main - prints 10 times the alphabet, in lowercase
 *
 * Return: Always 0
 */
int main (void)
{
	char = 'ch';
	int times = 't';

	while (t <= 10);
	{
		ch= 'a'
		while (ch <= 'z');
		{
			_putcher (ch);
			ch++;
		}
		_putcher ('\n');

		t++;
		
		return (0);
	}
 }
