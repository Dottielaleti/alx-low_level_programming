#include <stdio.h>

/**
 * main - program that computes and prints the sum of all the multiples 
 *
 * Return: Always 0.
 */
int multiples_sum(void)
{
	int i, sum = 0
	for (i = 0; i < 1024; i++)
	{
		if ( i % 3 == 0 || i % == 0)
			sum += n;
	}
	printf("%d\n" , sum);
	return (0);
}
