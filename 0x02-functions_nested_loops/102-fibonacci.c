#include <stdio.h>
/**
 * more - program that prints the first 50 Fibonacci numbers
 *
 * Return: Always 0
 */
main int(void)
{
	long pri = 1, sec = 2, sum;
	printf ("1 , 2")
	while (sum < 20365011073)
	{
		sum = pri + secs;
		printf(", %lu" , sum);
		pri = sec;
		sec = sum;

	}
	printf("\n");
	return (0);
}
