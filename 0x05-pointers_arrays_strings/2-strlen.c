#include "main.h"
/**
 *  _strlen - returns length of a string
 *  @s: character
 *
 *  Return: length of a string
 */
int _strlen(char *s)
{
	length = 0;

	while (*str++)
	length++;
	return (length);
}
